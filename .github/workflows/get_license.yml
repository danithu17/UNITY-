name: Get Unity License 🔑

on: workflow_dispatch

jobs:
  activation:
    name: Generate .alf file via Docker 🐳
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate Activation File
        run: |
          # Folder එකට පූර්ණ අවසර ලබා දීම
          chmod -R 777 $(pwd)
          
          docker run --rm \
            -v "$(pwd):/project" \
            -e "HOME=/tmp" \
            unityci/editor:ubuntu-2022.3.0f1-base-3 \
            unity-editor \
              -batchmode \
              -nographics \
              -createManualActivationFile \
              -logfile /dev/stdout \
              -projectPath /project
          
          # ඇත්තටම file එක හැදුණාද කියලා check කරලා බලන්න
          ls -R
          
          # Root permissions අයින් කරලා සාමාන්‍ය කෙනෙකුට ගන්න පුළුවන් විදිහට හැදීම
          sudo chmod -R 777 $(pwd)

      - name: Upload ALF
        uses: actions/upload-artifact@v4
        with:
          name: Unity-Activation-File
          # මෙතන අපි search කරනවා ඕනෑම තැනක .alf එකක් තියෙනවාද කියලා
          path: "**/*.alf"